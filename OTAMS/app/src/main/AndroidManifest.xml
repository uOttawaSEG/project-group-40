<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OTAMS">
        <activity
            android:name=".Layout.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StudentRegistration" />
        <activity
            android:name=".TutorRegistration" />


        <!-- Added for switching between activities -->
        <!-- Code now recognizes these layouts -->
        <activity android:name=".Layout.register" />
        <activity android:name=".Layout.login" />
        <activity android:name=".Layout.welcome" />
        <activity android:name=".Layout.registerAsStudent" />
        <activity android:name=".Layout.registerAsTutor" />

    </application>

</manifest>